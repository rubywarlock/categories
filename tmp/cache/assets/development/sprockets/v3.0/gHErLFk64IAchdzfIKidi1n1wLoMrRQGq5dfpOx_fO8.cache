I"*(function() {
  var root;

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  root.cur_attr = '';

  root.js_show = function(acc) {
    var attr, cur_attr;
    cur_attr = $(acc).attr("class");
    if (cur_attr !== "site-dropdown-menu site-dropdown-open") {
      $(".site-dropdown-open").slideToggle("fast");
      $(".site-dropdown-open").attr({
        "class": "site-dropdown-menu"
      });
      $(acc).slideToggle("fast");
      attr = $(acc).attr("class");
      $(acc).attr({
        "class": attr + ' ' + 'site-dropdown-open'
      });
    }
    if (cur_attr === "site-dropdown-menu site-dropdown-open") {
      $(".site-dropdown-open").slideToggle("fast");
      $(".site-dropdown-open").attr({
        "class": "site-dropdown-menu"
      });
    }
  };

}).call(this);
:ET